@model Lanche.ViewModels.HomeViewModel

<partial name="_Carousel" />


<h2>Os produtos da semana</h2>

<h4>Está com fome ??? </h4>

<div class="row">
        @foreach(var produto in Model.ProdutosPreferidos)
        { <partial name="_ProdutoResumo"for="@produto"/>} 
      </div>